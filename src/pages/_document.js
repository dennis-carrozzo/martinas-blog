import { Html, Head, Main, NextScript } from 'next/document'

/**
 * The above function is a JavaScript code that exports a default function called "Document" which
 * returns a JSX element representing the structure of an HTML document.
 */
export default function Document () {
  return (
    <Html lang='en'>
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}
